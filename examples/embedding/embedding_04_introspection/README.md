# Embedding Example 04 — CPU introspection (C++ API)

Shows how to load a tiny program, step it, and inspect:
- PC
- registers (u64 by index)
- flags
- tick count

## Run

From WSL:

```bash
make run
```

For extra step diagnostics:

```bash
./embedding_04_introspection --debug
```
