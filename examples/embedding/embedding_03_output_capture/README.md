# Embedding Example 03 — Output capture buffer (C API)

Shows how to enable the optional internal output capture buffer and read it after execution.

## Run

From WSL:

```bash
make run
```

For verbose hook/debug output:

```bash
./embedding_03_output_capture hello.hex --debug
```
